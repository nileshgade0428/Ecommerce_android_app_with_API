<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<!--     Cart Icon and Count-->
    <item
        android:id="@+id/action_cart"
        android:title="Cart"
        app:actionLayout="@layout/action_view_cart"
        app:showAsAction="always"
        android:icon="@drawable/cart_img" />

    <!-- Logo Icon -->
    <item
        android:id="@+id/action_logo"
        android:title="Logo"
        app:actionLayout="@layout/action_view_cart"
        app:showAsAction="always"
        android:icon="@drawable/logo" />

    <item
        app:showAsAction="always"
        android:icon="@drawable/ic_baseline_more_vert_24"
        android:title="">
        <menu>
            <item
                android:id="@+id/action_desc"
                android:title="Descending Order"
                app:showAsAction="never"/>
            <item
                android:id="@+id/action_asec"
                android:title="Ascending Order"
                app:showAsAction="never"/>
            <item
                android:id="@+id/action_limit"
                android:title="Limit 5"
                app:showAsAction="never"/>

        </menu>
    </item>

</menu>
